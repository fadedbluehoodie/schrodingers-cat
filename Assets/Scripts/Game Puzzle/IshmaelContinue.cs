using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IshmaelContinue : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        PageController.currentIndex = 9;
    }
}
